// Moneti v dgoba
using namespace std;
int main()
{

    float a, b, c, d, e, f;
    cout << "a=";
    cin >> a;

    cout << "b=";
    cin >> b;

    cout << "c=";
    cin >> c;

    cout << "d=";
    cin >> d;

    cout << "e=";
    cin >> e;

    cout << "f=";
    cin >> f;

    float sum = (a * 12) + (b * 7) + (c * 13) + (d * 5) + (e * 4) + (f * 8);

    cout << "sum=" << sum << endl;
    return 0;
}
