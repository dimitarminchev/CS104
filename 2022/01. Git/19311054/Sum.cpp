#include <iostream>
int main() {
    // Декларираме променливи и ги инициализираме
    int a = 2, b = 3;

    /* Декларираме промелива и я инициализираме със сумата на предходните 2
     * променливи
    */
    int c = a + b;

    // Извеждаме резултата на получената сума
    std::cout << "a+b=" << c << std::endl;
    return 0;
}